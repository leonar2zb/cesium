// This file is no longer used as the project has migrated to Next.js
// Keeping it for reference in case it's needed later
/*
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import StreamPixel from './StreamPixel'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <StreamPixel />
  </StrictMode>,
)
*/
